let active="All";const search=document.querySelector('#journal-search'),entries=[...document.querySelectorAll('#entries article')],buttons=[...document.querySelectorAll('[data-filter]')],empty=document.querySelector('#no-results');function render(){const query=(search.value||'').trim().toLowerCase();let count=0;entries.forEach(entry=>{const show=(active==='All'||entry.dataset.category===active)&&entry.textContent.toLowerCase().includes(query);entry.hidden=!show;if(show)count++});empty.hidden=count!==0}search.addEventListener('input',render);buttons.forEach(button=>button.addEventListener('click',()=>{active=button.dataset.filter;buttons.forEach(item=>item.classList.toggle('active',item===button));render()}));const menu=document.querySelector('#menu-button'),nav=document.querySelector('#journal-nav');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});
